{
    "cmd": [ "dd", "if=/dev/zero", "of=/tmp/zero", "bs=512K" ],
    "mounts": [
        { "type": "tmpfs", "dest": "/tmp" }
    ],
    "cgroupConfig": {
        "memory.max": "1000000",
        "memory.swap.max": "1000000"
    }
}
